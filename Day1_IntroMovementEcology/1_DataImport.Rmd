---
title: "M1: Data Import"
subtitle: "Day 1 — Introduction to Movement Ecology"
author: "Animal Movement Analyses for Ecologists | SYSU Shenzhen"
date: '`r format(Sys.time(), "%d %B %Y")`'
output:
  html_document:
    toc: true
    toc_depth: 4
    toc_float: 
      collapsed: true
      smooth_scroll: true
    number_sections: false
pdf_document: default
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, message = FALSE, warning = FALSE)
```

<a href="https://github.com/Animal-Movements/China_2026.git" class="github-corner" aria-label="View source on GitHub"><svg width="80" height="80" viewBox="0 0 250 250" style="fill:#151513; color:#fff; position: absolute; top: 0; border: 0; right: 0;" aria-hidden="true"><path d="M0,0 L115,115 L130,115 L142,142 L250,250 L250,0 Z"></path><path d="M128.3,109.0 C113.8,99.7 119.0,89.6 119.0,89.6 C122.0,82.7 120.5,78.6 120.5,78.6 C119.2,72.0 123.4,76.3 123.4,76.3 C127.3,80.9 125.5,87.3 125.5,87.3 C122.9,97.6 130.6,101.9 134.4,103.2" fill="currentColor" style="transform-origin: 130px 106px;" class="octo-arm"></path><path d="M115.0,115.0 C114.9,115.1 118.7,116.5 119.8,115.4 L133.7,101.6 C136.9,99.2 139.9,98.4 142.2,98.6 C133.8,88.0 127.5,74.4 143.8,58.0 C148.5,53.4 154.0,51.2 159.7,51.0 C160.3,49.4 163.2,43.6 171.4,40.1 C171.4,40.1 176.1,42.5 178.8,56.2 C183.1,58.6 187.2,61.8 190.9,65.4 C194.5,69.0 197.7,73.2 200.1,77.6 C213.8,80.2 216.3,84.9 216.3,84.9 C212.7,93.1 206.9,96.0 205.4,96.6 C205.1,102.4 203.0,107.8 198.3,112.5 C181.9,128.9 168.3,122.5 157.7,114.1 C157.9,116.9 156.7,120.9 152.7,124.9 L141.0,136.5 C139.8,137.7 141.6,141.9 141.8,141.8 Z" fill="currentColor" class="octo-body"></path></svg></a><style>.github-corner:hover .octo-arm{animation:octocat-wave 560ms ease-in-out}@keyframes octocat-wave{0%,100%{transform:rotate(0)}20%,60%{transform:rotate(-25deg)}40%,80%{transform:rotate(10deg)}}@media (max-width:500px){.github-corner:hover .octo-arm{animation:none}.github-corner .octo-arm{animation:octocat-wave 560ms ease-in-out}}</style>

# Learning Objectives

By the end of this module you will be able to:

1. Download animal tracking data directly from [Movebank](https://www.movebank.org/) using the `move2` package
2. Import data from a local CSV file as an alternative workflow
3. Understand the structure of a `move2` object
4. Perform initial exploration to understand your dataset

**Packages used:** `move2`, `tidyverse`, `lubridate`, `sf`, `mapview`, `units`

# Introduction
  >Example: White-bearded wildebeest (2010-2013)

Throughout this exercise, we will use a dataset on white-bearded wildebeest (*Connochaetes taurinus*) collected from 2010-2013 in southern Kenya. We will focus on getting these data into [R](https://cran.r-project.org/) in a clean and reproducible way, using data formats and functions introduced in the `move2` package.

<div style="float:right">
<img width="270" height="176" src="Figures/DSC_0287.jpg">
</div>

The data used in this exercise can be referenced as:

Stabach JA, Hughey LF, Crego RD, Fleming CH, Hopcraft JGC, Leimgruber P, Morrison TA, Ogutu JO, Reid RS, Worden JS, Boone RB. 2022. Increasing anthropogenic disturbance restricts wildebeest movement across East African grazing systems. Frontiers in Ecology and Evolution. [doi.org/10.3389/fevo.2022.846171](https://doi.org/10.3389/fevo.2022.846171)

Stabach JA, Hughey LF, Reid RS, Worden JS, Leimgruber P, Boone RB. 2020. Data from: Comparison of movement strategies of three populations of white-bearded wildebeest. Movebank Data Repository. [doi:10.5441/001/1.h0t27719](https://www.datarepository.movebank.org/handle/10255/move.1095)

# Setup
As a first step, we will import the necessary packages to conduct our analysis. This requires clearing your console and loading the required libraries for analysis.

```{r Load Libraries}
# Clear R's memory
rm(list=ls())

# You may need to install these packages first
# install.packages(c("move2", "tidyverse", "lubridate", "sf", "mapview", "units"))

library(move2)       # Movebank interface and the move2 spatial data class
library(tidyverse)   # Data wrangling and visualization (dplyr, ggplot2, etc.)
library(lubridate)   # Working with timestamps and timezones
library(sf)          # Spatial vector data (move2 is built on sf)
library(mapview)     # Quick interactive maps
library(units)       # Provides support for measurement units
```

## Setting the Time Zone and Coordinate System
Data in this example were collected in Kenya.  Data are stored in [Movebank](https://www.movebank.org/), however, are stored in UTC (Coordinated Universal Time).  This is standard practice, but means that data need to be converted to local time (East Africa Time - EAT) prior to conducting analyses.  A helpful [wiki](https://en.wikipedia.org/wiki/List_of_tz_database_time_zones) exists to help in choosing the appropriate timezone identifier for your study.

Data collected via a GPS device must be encoded with an appropriate projection system to link the coordinates to specific locations on the earth’s surface. When you acquire or download geospatial data, make sure you know the projection of the data.  This information is usually found in the metadata of a given dataset or should be provided by the person that is sharing the data with you. Checking and re-checking your dataset is very important. 

It is important to recognize that projections try to minimize distortion of a 3-dimensional object (the earth) on a 2-dimensional surface (paper map or computer screen). Choosing a projection that minimizes this distortion is especially important when doing calculations of area or speed. It is recommended to collect all GPS data in geographic coordinates and project into a coordinate system that minimizes distortion of your objects of interest.

<center>
![[Source: ESRI](https://www.esri.com/arcgis-blog/products/arcgis-pro/mapping/gcs_vs_pcs)](Figures/projections-esri-blog.png){width=75%}
</center>
<br>

The Universal Tranverse Mercator (UTM) projection is a common projection used for assigning coordinates to locations on the surface of the earth.  The UTM projection divides earth into 60 distinct zones, using the equator as the origin (0) and spanning 1,000,000 meters.  The center of the zone is defined as 500,000 meters.  As a result, you can identify any geographic point on earth to a specific UTM zone and meter coordinate.  In this example, we will re-project the data to UTM's. You can find the reference information for UTM zones listed in [http://spatialreference.org/ref/epsg/](http://spatialreference.org/ref/epsg/) and [https://epsg.io/](https://epsg.io/).

```{r Set UTM Zone}
# Set TimeZone
tz_utc <- "UTC"
tz_local <- "Africa/Nairobi"  # East Africa Time (EAT = UTC+3)

# Set UTM
latlong_crs <- "EPSG:4326"   # WGS84 geographic — standard for GPS and Movebank
utm_crs <- "EPSG:32737"  # UTM Zone 37S — metric, appropriate for Athi-Kaputiei, Kenya
```

> **NOTE**: The data we are using spans multiple UTM zones (UTM 36 south and UTM 37 south).  This should be avoided in most cases by choosing other coordinate systems, such as Lambert's or Alber's Equal-Area conic projections, that span multiple UTM zones. Doing so will minimize distortion.

# Downloading Data
We will demonstrate two methods for downloading data for analysis. In the first method, data are downloaded directly from [Movebank](https://www.movebank.org). In the second, data are downloaded from a .csv file. Both approaches produce the same end result. However, downloading data from [Movebank](https://www.movebank.org) offers additional advantages: it provides a standardized data structure and enables access to data that are continuously updated as new data are collected.

## Movebank
[Movebank](https://www.movebank.org) is the world's largest repository of animal tracking data, hosting data for thousands of studies across various taxa. The `move2` package provides a direct interface to [Movebank](https://www.movebank.org), allowing you to download data directly into [R](https://cran.r-project.org/) for analysis.  

### Store your credentials (once per computer)
Provide your credentials to access the [Movebank](https://www.movebank.org) API.

```{r Credentials, eval=FALSE}
# Run this once — credentials are stored securely in your system keychain
movebank_store_credentials("your_username", "your_password")
```

> **Security note:** Never hardcode your password in a script that you share or commit to GitHub. `movebank_store_credentials()` stores it once in your Operating System (OS) keychain. After saving once, `move2` functions will use it automatically.

### Explore available studies (optional)
See what's available on [Movebank](https://www.movebank.org) that is publicly accessible.  This is often more easily accomplished by logging on to [Movebank](https://www.movebank.org) and using the online interface.

```{r Search Studies, eval=FALSE}
# Search for open-data studies that you can download data from
movebank_download_study_info(
  license_type = "CC_0",
  i_have_download_access = TRUE,
  attributes = c("name", "id")
)
```

### Wildebeest Data Download
Use the function `movebank_download_study()` and specify the ID to download the wildebeest data from [Movebank](https://www.movebank.org).

```{r Download Movebank, eval=FALSE}
WB.mv2 <- movebank_download_study(
  study_id = 208413731 # Use the Movebank ID or the Study Name
  #study_id = "White-bearded wildebeest (Connochaetes taurinus) movements - Kenya"
)

# You may be prompted to accept the data license. If so, you can append the license number to your code and run again, like this:
WB.mv2 <- movebank_download_study(
  study_id = 208413731,
  #study_id = "White-bearded wildebeest (Connochaetes taurinus) movements - Kenya"
  'license-md5'='xxxxxx' # <- copy license number here
)
```

For these exercises, we will load a pre-downloaded version to avoid delays. This object is identical to what `movebank_download_study()` would return. 

```{r Load RData, echo=FALSE}
# Load
load("./Data/wildebeest_data.rdata")
```

## CSV Import
If you are unable to connect to [Movebank](https://www.movebank.org), or if your data exists elsewhere, you can import from a .CSV and convert to a `move2` object using `mt_as_move2()`.  Make sure you force the 'timestamp' field to be read as raw text ('col_character'), rather than letting `read_csv()` set the datetime and always verify that the timestamp was imported properly.   

```{r csv_import, eval=FALSE}
# Read file
# Change the date format (here ymd_hms) to match with your dataset
WB.csv <- read_csv("Data/WB_FullDataset.csv",
                   col_types = cols(timestamp = col_character())) %>% 
  mutate(timestamp = ymd_hms(timestamp, tz = tz_utc))

# Always verify the timestap before moving on
# The result of this query should be 0:
# sum(is.na(WB.csv$timestamp))

# Convert to move2 object
WB.csv <- mt_as_move2(WB.csv, 
                      coords = c("longitude", "latitude"),  # column names for x, y
                      crs = latlong_crs,
                      time_column = "timestamp",
                      track_id_column  = "individual_local_identifier"
                      )

# Note, the reference information was not included in the folder.  If included, they would need to be joined together based on a common field.  This could be easily accomplished using the `left_join()` function on a shared field (e.g., ID).
```

> **Note:** `mt_as_move2()` works with any data frame that has coordinates and timestamps. Once converted, the object behaves identically to a Movebank download — all the same `mt_*` functions apply.

# Understanding the Move2 Object
`move2` is a relatively new package (2022) designed to make working with tracking data more intuitive. A `move2` object is fundamentally an `sf` (simple features) object — meaning every row already carries a spatial point geometry — with extra structure to handle multiple individuals and timestamps. Using `move2` will be useful as we explore other analyses that have been developed to work with the library.

### Data Summary
Summarizing the data will provide you with the number of tracks, time range of data collection, the Coordinate reference systemS, and a preview of the data.

```{r class}
# Print the move2 object class
class(WB.mv2)   # move2, sf, tbl_df, tbl, data.frame
# Print a structured summary
WB.mv2
```

### Functions for Summarizing a Move2 Object

```{r mt_functions}
# Number of unique animals - How many distinct individuals are there that have a track
n_distinct(mt_track_id(WB.mv2))

# Number of tracks
mt_n_tracks(WB.mv2)

# Name of timestamp column
mt_time_column(WB.mv2)

# Name of track ID column
mt_track_id_column(WB.mv2)

# Overall time range
range(mt_time(WB.mv2), na.rm = TRUE)
```

### Event Data vs Track Metadata
A `move2` object holds two distinct types of information:

1. Event data: One row per GPS fix — location, timestamp, sensor readings
2. Track metadata: One row per individual — sex, deployment dates, study site

```{r track_data}
# Individual-level metadata (deployment information)
track_info <- mt_track_data(WB.mv2)
glimpse(track_info)
```

```{r column_names}
# Column names in the event data
names(WB.mv2)
```

> When you download from Movebank with `move2`, the deployment metadata (individual info, study site, sex, deploy on/off dates) is automatically embedded in the track data. In Module 2, we will use this information to subset to the study area and filter by deployment period.



# Initial Data Exploration
Explore the basic properties of the dataset, querying the dimensions and structure of the data collected.

```{r dimensions}
nrow(WB.mv2) # Total number of rows in the dataset
ncol(WB.mv2) # Total number of columns in the dataset
dim(WB.mv2) # Total number of rows and columns
glimpse(WB.mv2) # Get a quick view of each column
```

## Per-individual Summary
Here, we will use `summarise()` to create a query of the data collected, summarizing how many fixes were collected for each animal and for how long. 

```{r individual_summary}
animal_summary <- WB.mv2 %>%
  as_tibble() %>%
  summarise(n_fixes = n(), 
            first_fix = min(timestamp, na.rm = TRUE), 
            last_fix = max(timestamp, na.rm = TRUE), 
            duration_d = round(as.numeric(difftime(max(timestamp, na.rm = TRUE),
                                                   min(timestamp, na.rm = TRUE),
                                                   units = "days")), 1),
            .by = individual_local_identifier) %>%
  arrange(individual_local_identifier)

animal_summary
```

> **Discussion:** Notice the variation in tracking duration and number of fixes across individuals. This kind of imbalance is common in real tracking datasets and has implications for comparative analyses. What might cause an individual to have fewer fixes than others?

## Sampling interval
Many movement analyses assume a regular sampling interval (or at least close to regular). It is important to understand your data's fix rate distribution before proceeding.  Calculate the time between consecutive fixes for each animal and summarize as a histogram.

```{r sampling_interval}
# mt_time_lags() returns the time difference to the NEXT location for each row in the dataset
time_lags <- WB.mv2 %>%
  arrange(individual_local_identifier, timestamp) %>%
  mutate(dt_hours = mt_time_lags(.) %>% # default to minutes
           set_units("hours") %>%  # convert to hours
           as.numeric()) %>%
  as_tibble()

# The last time lag of each individual track will always be NA, so the total NA should be equal to the number of individuals and needs to be removed for calculations
sum(is.na(time_lags$dt_hours)) 

# Use filter to remove NA values
time_lags <- time_lags %>%
  filter(!is.na(dt_hours))

# What is the nominal sampling interval? Different tags/studies/species can have very different fix schedules.
target_interval_h <- round(median(time_lags$dt_hours), 1)
cat("Empirical (median) sampling interval across all individuals:", target_interval_h, "hour\n")

# Summary of sampling intervals per individual
time_lags %>%
  summarise(
    median_interval_h = round(median(dt_hours), 2),
    min_interval_h = round(min(dt_hours), 2),
    max_interval_h = round(max(dt_hours), 2),
    pct_irregular = round(mean(abs(dt_hours - target_interval_h) > 0.1) * 100, 1),
    .by = individual_local_identifier
  )

# Distribution of intervals for one individual
time_lags %>%
  filter(individual_local_identifier == first(individual_local_identifier),
         dt_hours < 12) %>%
  ggplot(aes(x = dt_hours)) +
  geom_histogram(binwidth = 0.25, fill = "steelblue", color = "white") +
  geom_vline(xintercept = target_interval_h, color = "firebrick", linetype = "dashed") +
  labs(title = "Distribution of fix intervals",
       subtitle = paste0("Red line = ", target_interval_h, "-hour target interval (empirical median)"),
       x = "Time between fixes (hours)", y = "Count") +
  theme_minimal()
```

This graph shows that the sampling intervals peak at 1 and 3 hours. In this case, the resulting sampling interval is by design - Wildebeest are more active during the day. As a result, collars were programmed to collect hourly fixes during daylight hours and every 3 hours at night, a duty cycle that collects relatively high resolution data during the day while saving battery overnight. We can check that this two-peak pattern really does line up with time of day, rather than just being an artifact of the histogram's binning.

```{r interval_by_hour}
# Tag each interval by the local hour of day of the fix that starts it, and
# check whether short (~1h) vs. long (~3h) intervals separate by time of day.
time_lags <- time_lags %>%
  mutate(hour_local = hour(with_tz(timestamp, tz_local)))

time_lags %>%
  filter(dt_hours < 6) %>%
  ggplot(aes(x = hour_local, y = dt_hours)) +
  geom_jitter(alpha = 0.1, width = 0.3, height = 0, color = "steelblue") +
  geom_hline(yintercept = c(1, 3), color = "firebrick", linetype = "dashed") +
  scale_x_continuous(breaks = seq(0, 23, 3)) +
  labs(title = "Fix interval vs. local hour of day",
       subtitle = "Red lines mark the 1-hour and 3-hour nominal intervals",
       x = "Hour of day (Africa/Nairobi)", y = "Time to next fix (hours)") +
  theme_minimal()
```

The ~1-hour intervals cluster during daylight hours and the ~3-hour intervals cluster overnight, confirming the duty cycle described, not random noise. Not every irregularity in tracking data is designed, however. 

A single `max_interval_h` will almost always look alarming. Over a multi-year deployment, nearly every individual will have some unusually long gap somewhere in its track. This isn't necessarily evidence of a problem. The more useful question is *whether long gaps are isolated to a single tag or shared across many individuals at the same time*. An isolated gap on one animal, on a random date, points to a tag- or animal-specific cause (battery, poor satellite geometry, dense cover). The same gap appearing on many animals at once, all resolving within the same few days, points to a system-wide cause instead — a ground-station/uplink outage or a network-wide duty-cycle change.

```{r gap_diagnostics}
# Flag long gaps (> 1 day) and ask: Are these gaps spread randomly across individuals and dates? Or, do many individuals lose/regain signal together?
gap_threshold_h <- 24

long_gaps <- time_lags %>%
  filter(dt_hours > gap_threshold_h) %>% # Investigate all records where the time lag is > 24 hours
  mutate(gap_start = timestamp,                       # last fix before signal was lost
         gap_end   = timestamp + dhours(dt_hours)) %>% # first fix after signal returned
  select(individual_local_identifier, gap_start, gap_end, dt_hours)

cat(nrow(long_gaps), "gaps longer than", gap_threshold_h, "hours, across",
    n_distinct(long_gaps$individual_local_identifier), "of",
    n_distinct(time_lags$individual_local_identifier), "individuals\n")

# Bin gap *resolutions* (gap_end) by week: a spike in the number of distinct individuals resolving a long gap in the same week is the signature of a synchronized, fleet-wide event rather than independent tag failures.
synchronized_gaps <- long_gaps %>%
  mutate(resume_week = floor_date(gap_end, "week")) %>%
  group_by(resume_week) %>%
  summarise(n_individuals_affected = n_distinct(individual_local_identifier),
            n_gaps = n(), .groups = "drop") %>%
  arrange(desc(n_individuals_affected))

synchronized_gaps %>% slice_head(n = 5)
```

```{r outage_window}
# Treat the week with the most individuals affected as a candidate system-wide outage (require >= 5 individuals so isolated coincidences don't get flagged). Then recover the full span -- earliest signal loss to latest signal recovery -- so that the temporal coverage can be plotted.
outage_start <- as.POSIXct(NA)
outage_end <- as.POSIXct(NA)

if (nrow(synchronized_gaps) > 0 & synchronized_gaps$n_individuals_affected[1] >= 5) {
  outage_week <- synchronized_gaps$resume_week[1]
  outage_gaps <- long_gaps %>% filter(floor_date(gap_end, "week") == outage_week)

  outage_start <- min(outage_gaps$gap_start)
  outage_end   <- max(outage_gaps$gap_end)

  cat("Candidate synchronized outage:", as.character(outage_start), "to",
      as.character(outage_end), "--", n_distinct(outage_gaps$individual_local_identifier),
      "individuals affected\n")
}
```

> **What this reveals:** a cluster of individuals all lose and regain signal within the same few-day window — far too many, and too synchronized, to be independent animal behavior or coincidental tag failures. That pattern is the signature of a real telemetry event (e.g., an uplink or ground-station outage), not a data error — it's genuine missing data, not a duplicate or invalid fix. It does need to be respected downstream: when we compute step lengths and movement metrics later in the workshop, a multi-day gap should never be treated as a single "step".

## Temporal coverage

```{r temporal_plot}
# Setup a blank graph with all indiviudals
p <- WB.mv2 %>%
  as_tibble() %>%
  mutate(date = as_date(timestamp)) %>%
  count(individual_local_identifier, date) %>%
  ggplot(aes(x = date, y = individual_local_identifier, fill = n))

# Create a rectangular to place on the graph if a synchronized outage window occurred
if (!is.na(outage_start)) {
  p <- p +
    annotate("rect",
             xmin = as_date(outage_start), xmax = as_date(outage_end),
             ymin = -Inf, ymax = Inf,
             fill = "red", alpha = 0.15)
}

# Graph the number of fixes collected over time for each animal
p +
  geom_tile() +
  scale_fill_viridis_c(name = "Fixes/day", option = "plasma") +
  labs(title = "Temporal coverage — Wildebeest GPS data",
       subtitle = if (!is.na(outage_start)) "Shaded band = candidate synchronized outage" else NULL,
       x = NULL, y = NULL) +
  theme_minimal() +
  theme(axis.text.x = element_text(angle = 45, hjust = 1))
```

>This plot gives an immediate visual of data gaps, deployment start/end dates, and variation across individuals. The shaded red band marks the candidate system-wide outage detected — notice it cuts vertically across *many* individual's. That's the visual signature of a shared, external cause, as opposed to a gap confined to a single row, which points to a tag- or animal-specific issue instead.

# Graph the Data Collected
Before doing any cleaning or analysis, always visualize your raw data. This is the fastest way to spot obvious errors — locations in the ocean, impossible coordinates, or fixes far outside the study area.  The `mapview` package provides tools that allow for interactive mapping.  To do so, we need to first convert te object to a simple features object (`sf`).

```{r quick_map}
# Convert to sf
WB.sf <- sf::st_as_sf(as.data.frame(WB.mv2))

# Create graph, plotting a subset of the data for speed
WB.sf %>%
  slice_sample(n = 2000) %>%    # random subset for speed
  mapview(zcol = "individual_local_identifier",
          layer.name = "Individual",
          cex = 2,
          alpha = 0.7)
```

> **Question:** Do all points fall within the expected study area? Are there any obvious outliers far from the main cluster? Do individual clusters seem reasonable for wildebeest?

# Save
Save the raw `move2` object so that we can use it in future exercises.  

```{r Save}
save(WB.mv2, file = "Output/WB_raw.rdata")
cat("Saved WB.mv2 —", n_distinct(mt_track_id(WB.mv2)), "animals,", nrow(WB.mv2), "fixes\n")
```

# Exercise: Apply to your own data
Now that you have uploaded and manipulated the wildebeest dataset in [R](https://cran.r-project.org/), adapt the code with your own data.  Use [Movebank](https://www.movebank.org/) to download an available dataset or upload your data directly using `read_csv()`.  Please do the following:

1. If you are pulling data from [Movebank](https://www.movebank.org/), use `movebank_download_study()` to download it. Run `n_distinct(mt_track_id())`, `mt_track_data()`, and create an individual summary table.
2. If you are importing data from a .csv,  use `mt_as_move2()`. Make sure your timestamp column is parsed correctly — check the `class()` of your object and confirm that the timestamp column is formatted as `POSIXct()`.
3. Run the temporal coverage heatmap and quickly map your own data. 

**Useful questions to answer for your own dataset:**
* How many individuals were imported? How long was each individual tracked?
* What is the sampling interval of your data? Is it regularly sampled?
* Are there any obvious spatial outliers?  Do you notice any gaps or suspicious points?
* What patterns do you notice?
